# ── Web framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# ── Async HTTP ──
httpx>=0.27.0
aiohttp>=3.9.0

# ── LLM clients ──
openai>=1.0.0

# ── Redis (optional — session persistence) ──
redis>=5.0.0

# ── Knowledge / RAG (optional — for production retrieval) ──
# Uncomment for production:
# rank-bm25>=0.2.2
# sentence-transformers>=2.7.0
# faiss-cpu>=1.8.0
# jieba>=0.42.1

# ── MCP (Model Context Protocol) ──
# mcp>=1.0.0
# fastmcp>=2.0.0

# ── RAG evaluation (optional) ──
# ragas>=0.2.0

# ── MCP dependencies ──
# qqmusic-api>=0.2.0

# ── Testing ──
pytest>=8.0.0
httpx>=0.27.0
